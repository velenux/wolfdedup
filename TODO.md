 * Add sqlite (in memory) code
 * Add an index to size field
 * Rewrite the script to read paths from the command line
 * Gracefully handle failed links
 * Add more options (eg. work only on files larger than 10k)


